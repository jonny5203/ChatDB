[packages]
fastapi = "*"
uvicorn = "*"
kafka-python = "*"
psycopg2-binary = "*"
sqlalchemy = "*"

[dev-packages]

[requires]
python_version = "3.11"

[scripts]
start = "uvicorn main:app --host 0.0.0.0 --port 8000"